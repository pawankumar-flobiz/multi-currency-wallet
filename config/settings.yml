jwt:
  secret: <%= ENV.fetch('JWT_SECRET') %>
  expiry: <%= ENV.fetch('JWT_EXPIRY') %>

redis:
  url: <%= ENV.fetch('REDIS_URL', 'redis://localhost:6379/1') %>
  otp_expiry: <%= ENV.fetch('REDIS_OTP_EXPIRY', 300) %>